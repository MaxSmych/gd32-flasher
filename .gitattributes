# Пакет драйвера подписан: подпись в .cat считается по содержимому файлов, включая
# .inf. Любая нормализация переводов строк ломает её, и Windows отказывается ставить
# драйвер. Забираем эту папку из-под autocrlf целиком.
tools/stlink-driver/** -text
